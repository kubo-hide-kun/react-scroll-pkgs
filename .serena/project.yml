# Serena Project Configuration for react-scroll-pkgs
# Turborepo monorepo with TypeScript/React packages

# list of languages for which language servers are started; choose from:
#   al                  bash                clojure             cpp                 csharp
#   csharp_omnisharp    dart                elixir              elm                 erlang
#   fortran             fsharp              go                  groovy              haskell
#   java                julia               kotlin              lua                 markdown
#   matlab              nix                 pascal              perl                php
#   powershell          python              python_jedi         r                   rego
#   ruby                ruby_solargraph     rust                scala               swift
#   terraform           toml                typescript          typescript_vts      vue
#   yaml                zig
# (This list may be outdated. For the current list, see values of Language enum here:
# https://github.com/oraios/serena/blob/main/src/solidlsp/ls_config.py
# For some languages, there are alternative language servers, e.g. csharp_omnisharp, ruby_solargraph.)
# Note:
#   - For C, use cpp
#   - For JavaScript, use typescript
#   - For Free Pascal/Lazarus, use pascal
# Special requirements:
#   - csharp: Requires the presence of a .sln file in the project folder.
#   - pascal: Requires Free Pascal Compiler (fpc) and optionally Lazarus.
# When using multiple languages, the first language server that supports a given file will be used for that file.
# The first language is the default language and the respective language server will be used as a fallback.
# Note that when using the JetBrains backend, language servers are not used and this list is correspondingly ignored.
languages:
- typescript

# the encoding used by text files in the project
# For a list of possible encodings, see https://docs.python.org/3.11/library/codecs.html#standard-encodings
encoding: "utf-8"

# whether to use project's .gitignore files to ignore files
ignore_all_files_in_gitignore: true

# list of additional paths to ignore in all projects
# same syntax as gitignore, so you can use * and **
# Turborepo のビルド成果物やキャッシュを無視
ignored_paths:
  - "**/lib/**"
  - "**/build/**"
  - "**/.turbo/**"
  - "**/node_modules/**"
  - "**/.next/**"
  - "**/out/**"
  - "**/coverage/**"
  - "**/*.jpg"
  - "**/*.avif"
  - "**/*.webp"
  - "**/dist/**"

# whether the project is in read-only mode
# If set to true, all editing tools will be disabled and attempts to use them will result in an error
# Added on 2025-04-18
read_only: false

# list of tool names to exclude. We recommend not excluding any tools, see the readme for more details.
# Below is the complete list of tools for convenience.
# To make sure you have the latest list of tools, and to view their descriptions, 
# execute `uv run scripts/print_tool_overview.py`.
#
#  * `activate_project`: Activates a project by name.
#  * `check_onboarding_performed`: Checks whether project onboarding was already performed.
#  * `create_text_file`: Creates/overwrites a file in the project directory.
#  * `delete_lines`: Deletes a range of lines within a file.
#  * `delete_memory`: Deletes a memory from Serena's project-specific memory store.
#  * `execute_shell_command`: Executes a shell command.
#  * `find_referencing_code_snippets`: Finds code snippets in which the symbol at the given location is referenced.
#  * `find_referencing_symbols`: Finds symbols that reference the symbol at the given location (optionally filtered by type).
#  * `find_symbol`: Performs a global (or local) search for symbols with/containing a given name/substring (optionally filtered by type).
#  * `get_current_config`: Prints the current configuration of the agent, including the active and available projects, tools, contexts, and modes.
#  * `get_symbols_overview`: Gets an overview of the top-level symbols defined in a given file.
#  * `initial_instructions`: Gets the initial instructions for the current project.
#     Should only be used in settings where the system prompt cannot be set,
#     e.g. in clients you have no control over, like Claude Desktop.
#  * `insert_after_symbol`: Inserts content after the end of the definition of a given symbol.
#  * `insert_at_line`: Inserts content at a given line in a file.
#  * `insert_before_symbol`: Inserts content before the beginning of a definition of a given symbol.
#  * `list_dir`: Lists files and directories in the given directory (optionally with recursion).
#  * `list_memories`: Lists memories in Serena's project-specific memory store.
#  * `onboarding`: Performs onboarding (identifying the project structure and essential tasks, e.g. for testing or building).
#  * `prepare_for_new_conversation`: Provides instructions for preparing for a new conversation (in order to continue with the necessary context).
#  * `read_file`: Reads a file within the project directory.
#  * `read_memory`: Reads the memory with the given name from Serena's project-specific memory store.
#  * `remove_project`: Removes a project from the Serena configuration.
#  * `replace_lines`: Replaces a range of lines within a file with new content.
#  * `replace_symbol_body`: Replaces the full definition of a symbol.
#  * `restart_language_server`: Restarts the language server, may be necessary when edits not through Serena happen.
#  * `search_for_pattern`: Performs a search for a pattern in the project.
#  * `summarize_changes`: Provides instructions for summarizing the changes made to the codebase.
#  * `switch_modes`: Activates modes by providing a list of their names
#  * `think_about_collected_information`: Thinking tool for pondering the completeness of collected information.
#  * `think_about_task_adherence`: Thinking tool for determining whether the agent is still on track with the current task.
#  * `think_about_whether_you_are_done`: Thinking tool for determining whether the task is truly completed.
#  * `write_memory`: Writes a named memory (for future reference) to Serena's project-specific memory store.
excluded_tools: []

# initial prompt for the project. It will always be given to the LLM upon activating the project
# (contrary to the memories, which are loaded on demand).
initial_prompt: |
  # Project: react-scroll-pkgs (Turborepo monorepo)
  
  ## Project Structure
  - **apps/website**: Demo/documentation site (not published, hosted at https://react-scroll-pkgs.vercel.app/)
  - **packages/***: Publishable libraries to npm
  
  ## Published Packages
  - `react-scroll-flip-book` (v1.0.2): React Hook for scroll flip book functionality
    - npm: https://www.npmjs.com/package/react-scroll-flip-book
    - Install: `npm install react-scroll-flip-book`
  - `use-window-scroll-in-element` (v1.1.1): React Hook for get window position of element
    - npm: https://www.npmjs.com/package/use-window-scroll-in-element
    - Install: `npm install use-window-scroll-in-element`
  
  ## Project Links
  - GitHub: https://github.com/kubo-hide-kun/react-scroll-pkgs
  - Demo Site: https://react-scroll-pkgs.vercel.app/ (hosted on Vercel)
  
  ## Important Rules
  1. **Minimize scope**: Changes across apps and packages require explanation of necessity first.
  2. **Publish impact**: Changes affecting npm publish must explicitly state impact on package.json/exports/build.
  3. **Lockfile**: Do not touch package-lock.json unless adding/updating dependencies.
  4. **API changes**: Public API changes (function signatures, exports, behavior) must document compatibility, migration steps, and release type (patch/minor/major).
  
  ## Build & Publish Workflow
  - Each package auto-builds via `prepublishOnly` script
  - Build output: `lib/` directory (TypeScript compilation)
  - Published files: `lib/`, `LICENSE` only (controlled by `files` field)
  - GitHub Actions: `npm run build` → `npm publish -w <package-name>`
  - **Critical**: Build task failure breaks entire publish workflow
  
  ## Turborepo Commands
  - `turbo run build` - Build all packages (respects dependencies)
  - `turbo run lint` - Lint all packages
  - `turbo run lint:fix` - Auto-fix linting issues
  - `turbo run typecheck` - Type check all packages
  
  ## Code Style
  - TypeScript/React preferred
  - Prettier: `singleQuote: true`, `arrowParens: always`
  - Use Prettier for formatting, do not manually format
  
  ## Commit Convention
  - Conventional commits format required
  - Scope is mandatory (`all` or package name)
  - Example: `feat(react-scroll-flip-book): add new feature`
  
  ## MCP (Model Context Protocol) Usage
  - **Use MCP tools extensively**: This project has multiple MCP Servers configured
  - **Priority order**:
    1. Code search/symbol operations: Use `serena` tools (`find_symbol`, `find_referencing_symbols`, `get_symbols_overview`)
    2. Web app verification: Use `playwright` or `chrome-devtools` for browser testing
    3. Code understanding: Use `cipher` for semantic search
    4. Traditional tools (grep, read_file): Use only as fallback when MCP cannot handle
  
  ## Validation Checklist (Fixed Order)
  1. `turbo run lint` - Check linting errors
  2. `turbo run typecheck` - Check type errors
  3. `turbo run build` - Verify build success (critical for publish workflow)
  4. `apps/website` verification - Use MCP `playwright` or `chrome-devtools` for browser verification
  
  ## Output Format
  Always provide: "Plan (max 5 steps)" → "Diff" → "Verification Results" → "Remaining Risks"

project_name: "react-scroll-pkgs"
included_optional_tools: []
